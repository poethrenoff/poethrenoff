sonata_admin:
    title: 'PoetHrenoff Admin'
    options:
        confirm_exit: false
    dashboard:
        blocks:
            - { position: left, type: sonata.admin.block.admin_list, settings: { groups: [admin] } }
