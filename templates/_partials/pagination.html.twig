{% if pagination is defined and pagination.total_pages > 1 %}
    {% set route = route|default(app.request.attributes.get('_route')) %}
    {% set query_params = app.request.query.all|merge(route_params|default({})) %}

    <div class="pagination">
        {% if pagination.prev_page %}
            <a href="{{ path(route, query_params|merge({'page': pagination.prev_page})) }}" class="pagination-link">&laquo; Назад</a>
        {% endif %}

        {% for page in range(1, pagination.total_pages) %}
            {% if page == pagination.current_page %}
                <span class="pagination-current">{{ page }}</span>
            {% elseif page in [pagination.current_page - 2, pagination.current_page - 1, pagination.current_page + 1, pagination.current_page + 2] %}
                <a href="{{ path(route, query_params|merge({'page': page})) }}" class="pagination-link">{{ page }}</a>
            {% elseif page == 1 or page == pagination.total_pages %}
                <a href="{{ path(route, query_params|merge({'page': page})) }}" class="pagination-link">{{ page }}</a>
            {% elseif page == pagination.current_page - 3 or page == pagination.current_page + 3 %}
                <span class="pagination-ellipsis">...</span>
            {% endif %}
        {% endfor %}

        {% if pagination.next_page %}
            <a href="{{ path(route, query_params|merge({'page': pagination.next_page})) }}" class="pagination-link">Вперед &raquo;</a>
        {% endif %}
    </div>
{% endif %}
