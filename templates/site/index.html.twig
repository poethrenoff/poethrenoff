{% extends 'site/base.html.twig' %}

{% block title %}Поэт Хренов{% endblock %}

{% block body %}
    <div class="columns-2">
        {% for group in favoriteGroups %}
            <div class="padding-main">
                <a href="{{ path('site_group', {'id': group.id}) }}">{{ group.title }}</a> ({{ group.comment }})
            </div>
        {% endfor %}
    </div>
{% endblock %}
