{% extends 'site/base.html.twig' %}

{% block title %}Поэт Хренов :: Картинки{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/glightbox/dist/css/glightbox.min.css" />
{% endblock %}

{% block body %}
<div class="container mx-auto p-4">

    <h1 class="text-3xl font-bold mb-4">Галерея</h1>

    {# Grid of thumbnails #}
    <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-2">
        {% for picture in pictures %}
            <div class="aspect-square overflow-hidden rounded shadow-md">
                <a href="{{ picture.imagePath }}" class="glightbox" data-gallery="gallery1" data-title="{{ picture.title|default('') }}">
                    <img
                        src="{{ picture.imagePath }}"
                        alt="{{ picture.title|default('Gallery image') }}"
                        class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
                        loading="lazy"
                    />
                </a>
            </div>
        {% endfor %}
    </div>

    {# Pagination #}
    {% if pictures is not empty and pagination is defined %}
        <div class="mt-8 flex justify-center">
            {% if pagination.prev_page %}
                <a href="{{ path('site_picture', {'page': pagination.prev_page}) }}" class="px-4 py-2 mx-1 rounded shadow bg-white hover:bg-gray-100">&laquo; Назад</a>
            {% endif %}

            {% for page in range(1, pagination.total_pages) %}
                {% if page == pagination.current_page %}
                    <span class="px-4 py-2 mx-1 rounded shadow bg-blue-500 text-white">{{ page }}</span>
                {% elseif page in [pagination.current_page - 2, pagination.current_page - 1, pagination.current_page + 1, pagination.current_page + 2] %}
                    <a href="{{ path('site_picture', {'page': page}) }}" class="px-4 py-2 mx-1 rounded shadow bg-white hover:bg-gray-100">{{ page }}</a>
                {% elseif page == 1 or page == pagination.total_pages %}
                     <a href="{{ path('site_picture', {'page': page}) }}" class="px-4 py-2 mx-1 rounded shadow bg-white hover:bg-gray-100">{{ page }}</a>
                {% elseif page == pagination.current_page - 3 or page == pagination.current_page + 3 %}
                    <span class="px-4 py-2 mx-1">...</span>
                {% endif %}
            {% endfor %}

            {% if pagination.next_page %}
                <a href="{{ path('site_picture', {'page': pagination.next_page}) }}" class="px-4 py-2 mx-1 rounded shadow bg-white hover:bg-gray-100">Вперед &raquo;</a>
            {% endif %}
        </div>
    {% endif %}
</div>
{% endblock %}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/gh/mcstudios/glightbox/dist/js/glightbox.min.js"></script>
    <script>
        const lightbox = GLightbox({
            selector: '.glightbox',
            touchNavigation: true,
            loop: true
        });
    </script>
{% endblock %}
